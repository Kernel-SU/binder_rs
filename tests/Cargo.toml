[package]
name = "binder-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
binder = { package = "binder_ndk", path = "../binder" }
binder_tokio = { path = "../binder_tokio" }
tokio = { version = "1.25.0", features = ["full"] }
